# Sesija 2026-07-14 — Veliki update (lockdown + nova mehanika rundi)

Sve niže je **deployano na laktarenje.com i verificirano uživo** (commiti `b335fb3` → `d544972`).

## 🔒 LOCKDOWN JE AKTIVAN

- Env var **`LAKAT_LOCKDOWN=1`** postavljen u Vercel Production.
- Svi osim **pgrbi0@gmail.com** i **test@mail.com** (allowlist hardkodiran u `proxy.js`) idu na `/uskoro` ("VELIKI UPDATE DOLAZI."). `/login` ostaje dostupan.
- **SVI pushevi pauzirani** (guard u `lib/push.js` setupWebpush — pokriva akcije i cronove).
- **Gašenje kad update bude spreman za ekipu:** obrisati `LAKAT_LOCKDOWN` u Vercel dashboardu (ili `vercel env rm LAKAT_LOCKDOWN production`) + redeploy. Sve se samo vrati, uklj. pushevi.

## 🍺 Nova mehanika rundi (TikTok stil)

- **TU SAM, "Ipak bježim", drink-bar i random kolo "Piće dana" — OBRISANI.**
- **Zeleni plus u sredini navbara** (svi ekrani, strši iz pilule): tap → kamera → photo editor (tekst na sliku) → Objavi → **omnitrix kolo pića** (`omnitrix.jsx`: vrtiš prstom, inercija + snap, piće pod kazaljkom, "Potvrdi" logira, X preskače; potpuno neproziran overlay).
- **Više rundi dnevno**: prva slika dana = check-in (jedina šalje push grupi), svaka sljedeća = nova slika + piće. Piće se logira ISKLJUČIVO kroz taj flow.
- **Undo** = toast "Zapisano: 🍺 Piva — ↩ Krivi tap" (8 s).
- Flow živi u `runda-flow.jsx` (lazy iz `nav.jsx`); overlayi idu `createPortal(document.body)` — **glass-nav backdrop-filter je containing block za fixed** (bug ulovljen i popravljen uživo).
- Statistika (pička mjeseca, streak) i dalje broji max 1 dan — netaknuto.

## 👉 Stižem na sliku

- Standalone "Stižem" gumb maknut; **"👉 Stižem" je na kartici prisutnog** (samo tuđe kartice, samo dok sam nisi za šankom/najavljen).
- Push ide **SAMO meti** (`notifyUser` + `najavaTargetPushBody`); ostali vide label **"Stiže kod X"** + badge **"👀 n stiže/stižu"** na kartici mete. Nema stižem dok nitko nije sjeo.
- **`supabase-najave2.sql` PRIMIJENJEN** u produkciji (kolona `najave.target_user_id` + index) — kroz Supabase dashboard, potvrđeno REST-om.

## 🎠 Carouseli (zamjerke iz iste večeri)

- **`photo-lightbox.jsx`** = multi-photo swipe carousel (CSS scroll-snap, točkice + brojač "2/5", body scroll lock, ghost-click zaštita; API: `items[{url, caption, checkinId?}]`, `startIndex`, children kao render-prop `(current) => ...`).
- **Slike dana grupirane PO KORISNIKU** — jedan kvadratić po korisniku (cover = zadnja slika, badge broja, badge reakcija = ZBROJ kroz sve slike), tap → fullscreen swipe, reakcije/komentari prate slajd. Sekcija preseljena u `sank.jsx` na realtime → **nova slika bez refresha**. `memorije.jsx` ostaje samo za flashbackove.
- **Galerija na profilu grupirana PO DANIMA** (`getDayKey`, 06:00–06:00) — kvadratić po danu, tap → swipe s "datum · vrijeme" captionima.

## ✨ UX popravci

- **Hint za plus** na Šanku kad nisi za šankom ("Slikaj dokaz i sjedni za šank. Stisni zeleni plus dolje. ↓").
- **Install banner** ima X koji se pamti (`localStorage: lakat-install-hint-dismissed`).
- **/shame legenda**: "Inventar mjeseca dolazi najviše, pička mjeseca najmanje."
- **Bedževi sklopivi** (`profil/badges-list.jsx`): otključani prvi + "Vidi sve (N)".

## 📋 Otvoreno za sljedeće sesije

1. **Korisnikov test na mobitelu**: cijeli flow (plus → slika → kolo → undo), stižem s dva accounta, više rundi u danu.
2. **Gašenje lockdowna** kad update bude spreman (vidi gore) — nakon toga provjeriti da pushevi rade (checkin push, stižem push meti).
3. **Testni podaci u TEST grupi**: 5 slika (UNDO TEST 1-3, DRUGA RUNDA, PRVA RUNDA BRAĆO) + 3 pića od verifikacije — obrisati iz baze ili ignorirati.
4. **Odgođeno (memory: lakat-mail-todo)**: Supabase SMTP support@laktarenje.com (Resend + DNS u cPanelu) + Site URL.
5. Sitno za razmotriti: omnitrix emojiji se rotiraju s kolom (nisu uspravni); "Objavljujem..." može potrajati par sekundi na sporoj mreži (nema progresa u %); Vercel Firewall challenge usporava automatizirane browsere (za prave korisnike nebitno).

## Tehnički podsjetnici

- SQL fajlovi se primjenjuju ručno u Supabase SQL editoru; zadnji primijenjen: `supabase-najave2.sql`. Tablica `kolo_spins` je napuštena ali ostaje u bazi.
- CLAUDE.md je ažuriran s novom mehanikom (pravila igre, realtime, env varijable).
- Dev server koristi PRODUKCIJSKU bazu — testni upisi samo u TEST grupu, nikad BETA.
